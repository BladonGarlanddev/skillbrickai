services:
  postgres:
    image: postgres:16
    container_name: skillbrickai-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-skillbrickai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-skillbrickai}
      POSTGRES_DB: ${POSTGRES_DB:-skillbrickai}
    ports:
      - '5435:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-skillbrickai} -d ${POSTGRES_DB:-skillbrickai}']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
